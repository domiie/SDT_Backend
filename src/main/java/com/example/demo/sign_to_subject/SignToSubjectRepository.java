package com.example.demo.sign_to_subject;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SignToSubjectRepository extends JpaRepository<SignToSubjectEntity, Long> {
}
